# Changesets

Use Changesets to describe versioned changes before publishing `graphtrace`.

Typical flow:

1. `pnpm changeset`
2. write the summary
3. merge changes
4. let the release workflow version and publish

